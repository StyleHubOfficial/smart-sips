16:    query, questionCount, subject, examType, classLevel, model, viewMode, difficulty, isPYQ, pyqYear, questionType, examFormat, isSmartPanelMode, sourceFile, questions, loading, selectedOptions, showSolutions, showHints, showSourceLinks, stepReveals, analysis, analyzing, whiteboardMode,
28:  const [slideModeConfig, setSlideModeConfig] = useState<{ questionsPerSlide: 1 | 2, overlay: boolean }>({ questionsPerSlide: 1, overlay: false });
51:    if (questions.length > 0 && Object.keys(selectedOptions).length === questions.length) {
52:      const score = questions.filter(q => selectedOptions[q.id] === q.correctAnswer).length;
53:      const percentage = (score / questions.length) * 100;
60:  }, [selectedOptions, questions]);
149:      const autoQuery = `Create practice questions for: ${extractedData?.topic || sourceContent.context?.custom?.title || 'this content'}. Focus on: ${extractedData?.keyConcepts?.join(', ') || ''}`;
246:        const allQs = usePracticeStore.getState().questions;
247:        usePracticeStore.setState({ questions: [] });
249:          usePracticeStore.setState(state => ({ questions: [...state.questions, q] }));
257:      addNotification('error', 'Failed to generate questions. Please check your API key and try again.');
278:      addNotification('success', 'Similar questions generated');
281:      addNotification('error', 'Failed to generate similar questions');
293:    if (questions.length === 0) return;
329:        <p>Generated by Smart Sunrise AI • ${questions.length} Questions • ${subject} • ${examType} • ${classLevel}</p>
332:    <div id="questions-container">
333:        ${questions.map((q, i) => `
378:      const contextStr = `title=${fallbackTitle} Practice Set|teacher=${role === 'teacher' ? 'Teacher' : role}|subject=${subject}|class=${classLevel}|description=Interactive practice questions for ${fallbackTitle} (${questionCount} Qs) - ${examType}|fileType=HTML Quiz`;
728:              {isPYQ ? 'Generate authentic previous year questions with search grounding' : 
729:               isSourceConverterMode ? (sourceFile ? `Converting ${sourceFile.name} into practice questions` : 'Select a source to convert into questions') : 
1080:            placeholder={isSourceConverterMode ? (sourceFile ? `Describe how to convert ${sourceFile.name}...` : 'Select a source first') : isPYQ ? `Which year's PYQs for ${examType} ${subject}?` : `e.g. ${questionCount} ${difficulty} questions of ${examType} in ${subject}...`}
1104:        {!loading && questions.length > 0 && (
1107:              Generated Questions <span className="text-gray-500 text-sm font-normal">({questions.length})</span>
1110:            {Object.keys(selectedOptions).length === questions.length && (
1113:                Score: {questions.filter(q => selectedOptions[q.id] === q.correctAnswer).length} / {questions.length}
1168:                  usePracticeStore.setState({ questions: [], selectedOptions: {}, showSolutions: {}, showHints: {}, stepReveals: {}, analysis: null });
1233:                  <button onClick={() => addNotification('info', 'Exporting solved questions...')} className="w-full text-left px-4 py-3 text-sm text-gray-300 hover:bg-white/5 hover:text-white transition-colors flex items-center gap-2">
1282:                  {questions.map((q, index) => (
1334:                      <p className="text-[10px] text-gray-500 mt-1 uppercase tracking-widest">Slide {currentSlide + 1} of {Math.ceil(questions.length / slideModeConfig.questionsPerSlide)}</p>
1342:                      onClick={() => setSlideModeConfig(prev => ({ ...prev, questionsPerSlide: 1 }))}
1343:                      className={`px-3 py-1.5 rounded-lg text-xs font-bold transition-all ${slideModeConfig.questionsPerSlide === 1 ? 'bg-[#00F0FF] text-black' : 'text-gray-400 hover:text-white'}`}
1348:                      onClick={() => setSlideModeConfig(prev => ({ ...prev, questionsPerSlide: 2 }))}
1349:                      className={`px-3 py-1.5 rounded-lg text-xs font-bold transition-all ${slideModeConfig.questionsPerSlide === 2 ? 'bg-[#00F0FF] text-black' : 'text-gray-400 hover:text-white'}`}
1378:                      onClick={() => setCurrentSlide(Math.min(Math.ceil(questions.length / slideModeConfig.questionsPerSlide) - 1, currentSlide + 1))}
1379:                      disabled={currentSlide === Math.ceil(questions.length / slideModeConfig.questionsPerSlide) - 1}
1400:                     {questions
1401:                       .slice(currentSlide * slideModeConfig.questionsPerSlide, (currentSlide + 1) * slideModeConfig.questionsPerSlide)
1404:                           {renderQuestionCard(q, currentSlide * slideModeConfig.questionsPerSlide + idx, true)}
1414:                         {Array.from({ length: Math.ceil(questions.length / slideModeConfig.questionsPerSlide) }).map((_, i) => (
1449:              {!loading && questions.map((q, index) => renderQuestionCard(q, index))}
1454:        {questions.length > 0 && !loading && (
1456:            {Object.keys(selectedOptions).length === questions.length && (
1460:                  {questions.filter(q => selectedOptions[q.id] === q.correctAnswer).length} / {questions.length}
1463:                  {Math.round((questions.filter(q => selectedOptions[q.id] === q.correctAnswer).length / questions.length) * 100)}% Accuracy
1515:        {questions.length === 0 && !loading && (
1518:            <p className="text-lg">Enter a topic above to generate practice questions</p>
